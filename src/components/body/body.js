import React from 'react';
import Login from '../auth/login';

const Body = props => {
  return(
    <section>
      <Login/>
      <p>This is the body part of our project</p>
    </section>
  )
}

export default Body;